package com.kusitms.connectdog.core.util

enum class AppMode {
    LOGIN,
    VOLUNTEER,
    INTERMEDIATOR
}

enum class UserType {
    SOCIAL_VOLUNTEER,
    NORMAL_VOLUNTEER,
    INTERMEDIATOR
}

enum class SocialType {
    GUEST,
    VOLUNTEER
}